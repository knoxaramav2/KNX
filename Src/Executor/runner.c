#include "executor.h"


token * interpretLevel(node *n, token * t){

    

    return NULL;
}

int execute(node * n){

    token * c = n->buffer.tokens;

    //token * res = interpretLevel(n, t);

    return 0;
}